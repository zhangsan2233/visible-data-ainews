# visible-data-ainews
 此Vue 项目打包后的静态资源包，需通过 HTTP 服务器运行（无法直接打开 index.html）
终端切换到 dist 目录：
cd 你的下载路径/dist
执行启动命令：
Python 2.x：python -m SimpleHTTPServer 8080
Python 3.x：python -m http.server 8080
浏览器访问 http://localhost:8080 即可。
